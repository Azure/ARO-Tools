$schema: ../config.schema.json
clouds:
  public:
    environments:
      int:
        defaults:
          geneva:
            actions:
              nonConflictingField: foo
          key1: '{{ .ctx.region }}-{{ .ctx.regionShort }}'
          key2: {{ .ev2.availabilityZoneCount }}
